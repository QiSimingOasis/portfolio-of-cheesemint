# system / editor
.DS_Store
.vscode/
.idea/

# Vercel local files
.vercel/
.vercel-data/

# Cloudflare local credentials
.wrangler-config/

# local tooling only (keep the deployed site purely static)
package.json
package-lock.json

# future Node dependencies (if the project is upgraded later)
node_modules/
dist/
.env
.env.*

# keep public examples if needed
!.env.example

.vercel
.env*
